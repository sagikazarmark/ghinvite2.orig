{
  "name": "ghinvite-worker-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node smoke.mjs",
    "test:deadlines": "node build.mjs && node deadlines.mjs && STALL_PHASE=body node deadlines.mjs",
    "test:admission": "node build.mjs && node build.mjs workflows && cargo build --locked -p ghinvite-github --features test-stub --example stub && node admission.mjs",
    "test:settlement": "SETTLEMENT_ONLY=1 npm run test:admission"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "0.27.3",
    "miniflare": "4.20260302.0"
  }
}
